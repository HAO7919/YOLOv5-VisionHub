# YOLOv5 可视化检测系统 - Python 依赖
# 注意: 建议使用 run.bat 自动安装,以下手动安装参考
#
# PyTorch (必须单独安装，根据是否有GPU选择):
#   CPU版: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   GPU版(CUDA 11.8): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   其他版本: 访问 https://pytorch.org/get-started/locally/ 获取对应命令

# GUI
PyQt5>=5.15.0

# 图像处理
opencv-python>=4.8.0
Pillow>=10.0.0
numpy==1.26.4

# 检测引擎
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# 工具库
tqdm>=4.65.0
pyyaml>=6.0
psutil>=5.9.0
