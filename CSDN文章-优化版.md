# 【开源】YOLOv5-VisionHub：桌面版YOLO目标检测工具，开箱即用，支持图片/视频/摄像头/屏幕实时检测

## 还在为装YOLO环境头疼？

学YOLO的人都懂，装环境能把人逼疯：
- Python版本不对
- PyTorch和CUDA版本不匹配
- 装完一堆依赖还是跑不起来
- 想给别人用，对方还得从头装一遍

我自己也踩过这些坑，所以干脆做了一个**桌面版YOLO目标检测工具**，不用装Python、不用装PyTorch、不用配任何环境，下载解压双击就能用。

GitHub地址：https://github.com/HAO7919/YOLOv5-VisionHub

## 能干嘛

### 四种检测模式

**图片检测**
打开图片一键检测，支持导出 JSON、CSV、VOC XML 格式结果，方便后续处理。

**视频检测**
逐帧检测，自动保存带检测框的结果视频，处理进度实时显示。

**摄像头实时检测**
独立线程运行，不卡界面，支持截图保存，接个摄像头就能实时看效果。

**屏幕实时检测**
实时捕获屏幕内容，叠加显示检测框，打游戏、看视频的时候都能实时检测。

### 多模型支持
- 支持 YOLOv5、YOLOv8、YOLOv9、YOLOv10、YOLOv11
- 自带4个官方模型（yolov5s/m/su/mu.pt），开箱即用
- 自己训练的 best.pt 放到 models 目录就能用，不用改代码

### 9种语言
中文、英文、日语、韩语、法语、德语、西班牙语、俄语、葡萄牙语，老外也能用。

### CPU/GPU自适应
- 有NVIDIA显卡自动用GPU加速，检测飞快
- 没有显卡自动用CPU，照样能跑，就是慢点
- 低配电脑也能流畅使用

### 参数可调
- 置信度阈值：滑杆拖动，实时生效
- IOU阈值：控制检测框重叠度
- 最大检测数量：防止检测框太多太乱
- 检测框线条粗细：想粗想细自己调

## 效果演示

（这里插你的截图：主界面、图片检测效果、视频检测、摄像头检测，至少3-5张）

## 怎么下载用

### 下载地址
GitHub Release：https://github.com/HAO7919/YOLOv5-VisionHub/releases/tag/v1.0.0

### 安装步骤
因为包比较大（约3GB），分成了两个压缩包，下载后合并一下：

1. 把 part1 和 part2 两个文件下载到同一个文件夹
2. 在该文件夹打开 CMD，运行：
```
copy /b YOLOv5-VisionHub-v1.0.0-part1.zip + YOLOv5-VisionHub-v1.0.0-part2.zip YOLOv5-VisionHub-v1.0.0.zip
```
3. 解压合并后的 zip
4. 双击 `YOLO检测工具.exe` 启动

**就这么简单，不用装任何东西。**

### 系统要求
- Windows 10 及以上（64位）
- 内存 4GB 以上（推荐 8GB）
- NVIDIA 显卡（可选，没有也能用 CPU）

## 用自己训练的模型

1. 把你训练好的 `.pt` 模型文件复制到软件目录下的 `models` 文件夹
2. 打开软件，在左侧「模型管理」里点「刷新列表」
3. 选中你的模型，点「切换到选中模型」
4. 等加载完成就能用了

## 技术栈

- Python 3.8
- PyQt5（界面）
- PyTorch + Ultralytics（检测引擎）
- OpenCV（图像处理）
- PyInstaller（打包成exe）

## 开源协议

MIT协议，可以自由使用、修改和分发，包括商用。觉得有用的话点个Star支持一下。

## 后续计划

- [ ] 模型训练功能集成（在软件里就能训练自己的模型）
- [ ] 更多导出格式支持
- [ ] 检测结果编辑功能（手动调整检测框）
- [ ] 批量处理功能（一次处理整个文件夹的图片）

欢迎 Star 和 Issue，有问题随时提！

GitHub地址：https://github.com/HAO7919/YOLOv5-VisionHub
